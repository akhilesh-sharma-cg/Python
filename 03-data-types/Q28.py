value = 10
print(type(value))

value = "Hello"
print(type(value))

# Explanation:
# First, value is an integer (int).
# After reassignment, value becomes a string (str).
# Python variables can change their data type when reassigned.